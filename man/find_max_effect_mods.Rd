% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_max_effect_mod.R
\name{find_max_effect_mods}
\alias{find_max_effect_mods}
\title{Identification of Maximal Effect Modifiers Using Stochastic Shift Interventions}
\usage{
find_max_effect_mods(
  at,
  av,
  deltas,
  a_names,
  w_names,
  outcome,
  outcome_type,
  mu_learner,
  g_learner,
  estimator = c("tmle", "onestep"),
  fluctuation = c("standard", "weighted"),
  top_n = 3,
  seed = NULL,
  min_obs,
  fold,
  density_classification = TRUE,
  adaptive_delta = FALSE,
  hn_trunc_thresh = 10,
  max_depth
)
}
\arguments{
\item{at}{A training-fold \code{data.frame} containing the baseline covariates, exposures, and outcome.}

\item{av}{A validation-fold \code{data.frame} with the same columns as \code{at}.}

\item{deltas}{A named \code{list} or \code{vector} specifying the shift in exposures to define the target parameter. Each element should correspond to an exposure variable specified in \code{a_names}, detailing the amount by which that exposure is to be shifted.}

\item{a_names}{A \code{character} vector specifying the names of the exposure variables.}

\item{w_names}{A \code{character} vector specifying the names of the covariate variables.}

\item{outcome}{The name of the outcome variable.}

\item{outcome_type}{A \code{character} string indicating the type of the outcome variable; either "continuous", "binary", or "count".}

\item{mu_learner}{A list of \code{\link[sl3]{Lrnr_sl}} learners specifying the ensemble machine learning models to be used for outcome prediction within the Super Learner framework.}

\item{g_learner}{A list of \code{\link[sl3]{Lrnr_sl}} learners specifying the ensemble machine learning models to be used for estimating the conditional density of the exposures.}

\item{estimator}{The type of estimator to fit: \code{"tmle"} or \code{"onestep"}.}

\item{fluctuation}{Method used in the targeting step: \code{"standard"} or \code{"weighted"}.}

\item{top_n}{An \code{integer} specifying the number of top positive and negative effects to return.}

\item{seed}{An \code{integer} value to set the seed for reproducibility.}

\item{min_obs}{Minimum number of observations in a region to warrant a split.}

\item{fold}{Label for the fold index (for cross-validation).}

\item{density_classification}{If TRUE, estimate the exposure density ratio via a classification reparameterization rather than direct conditional density estimation.}

\item{adaptive_delta}{If TRUE, reduce the exposure shift until the training-fold
clever covariate is no larger than \code{hn_trunc_thresh}.}

\item{hn_trunc_thresh}{Maximum allowed clever-covariate value under
\code{adaptive_delta}.}

\item{max_depth}{Maximum depth of the partitioning tree used to discover effect-modification regions.}
}
\value{
A list containing the top effects and interactions identified and estimated by the function. It includes elements for top positive and negative individual effects as well as top synergistic and antagonistic interactions.
}
\description{
The \code{find_max_effect_mods} function identifies subpopulations with the maximum differential impact of stochastic shift interventions on exposures within a mixture. The method estimates the individual effects of shifting each exposure while controlling for other exposures and covariates, using ensemble machine learning and targeted maximum likelihood estimation (TMLE) or a one-step estimator.
Once the individual intervention effects and influence curve estimates given an intervention on each exposure are derived, we then use a simple t-statistic partitioning algorithm to find the region with the maximum significant difference in intervention effects.
}
\examples{
\dontrun{
data <- data.frame(matrix(rnorm(100 * 10), ncol = 10))
names(data) <- c(paste0("X", 1:8), "exposure", "outcome")
deltas <- list(exposure = 0.1)
a_names <- "exposure"
w_names <- paste0("X", 1:8)
outcome <- "outcome"
outcome_type <- "continuous"
mu_learner <- list(sl3::Lrnr_mean$new(), sl3::Lrnr_glm$new())
top_n <- 3
seed <- 123

results <- find_max_effect_mods(
  at = data, av = data, deltas = deltas, a_names = a_names, w_names = w_names,
  outcome = outcome, outcome_type = outcome_type, mu_learner = mu_learner,
  g_learner = mu_learner, top_n = top_n, seed = seed, min_obs = 10,
  fold = 1, density_classification = TRUE, max_depth = 1
)
print(results)
}

}
